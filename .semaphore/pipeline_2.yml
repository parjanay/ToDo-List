version: v1.0
name: k8saas AKS Integration Tests
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: AKS Integration Test
    task:
      jobs:
        - name: AKS Integration Test
          commands:
            - 'if [ $((RANDOM % 2)) -eq 0 ]; then'
            - '  echo "Random fail ❌"'
            - '  exit 1'
            - else
            - '  echo "Random pass ✅"'
            - fi
